/**
 * Interface: Card. Plain text in; an op list, then build requests (Goals) out. Twelve ops: SET, ADD, SUB, AND, OR, NOT, JMP, JZ, WAIT, OUT, IN, NEED. No new ops until Phase 4 is accepted. Parse errors are reported with a line number and never throw past the parser.
 */
package net.tabor.seedcity.card;
