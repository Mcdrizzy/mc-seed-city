/**
 * Server config: max radius, max builders (default 6), max collectors (default 4), chunk cap, warding block, permitted collector sources, build rate. A forgotten seed must never eat a world.
 */
package net.tabor.seedcity.config;
