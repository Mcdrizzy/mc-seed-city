/**
 * Interface: Verify. A placement plus its neighbours in, pass/fail out. Headless tick simulator plus the named truth models (passthrough, not, and, or, register, counter, decoder, actuator, sensor, none). Results cached by (cell id, neighbour ids, orientation). The single most important piece of engineering in the project.
 */
package net.tabor.seedcity.verify;
