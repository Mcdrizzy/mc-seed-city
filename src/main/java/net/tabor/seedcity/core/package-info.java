/**
 * The Core owns CityState: seed origin, district list and priority order, frontier set, storage totals, current Program and its register bindings. Rule flows down from here as a single ordered district list, recomputed on each program change.
 */
package net.tabor.seedcity.core;
